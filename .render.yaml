services:
  - type: web
    name: core
    env: node
    rootDir: core
    buildCommand: pnpm install --frozen-lockfile --prod=false && pnpm exec tsc --listEmittedFiles || true && pnpm build && echo "dist files:" && find dist -type f -print || true
    startCommand: pnpm start
